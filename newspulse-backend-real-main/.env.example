# MongoDB Atlas connection string (recommended for production)
# Get this from: Atlas UI > Your Cluster > Connect > Drivers
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>.mongodb.net/newsdb?retryWrites=true&w=majority

# Local MongoDB (development fallback)
MONGO_URI=mongodb://127.0.0.1:27017/newsdb

# Server port (optional, defaults to 5000)
# PORT=5000

# Admin panel credentials (used by POST /admin/login)
# These must match what your admin UI expects for login.
FOUNDER_EMAIL=founder@example.com
FOUNDER_PASSWORD=replace-me
FOUNDER_NAME=NewsPulse Founder
FOUNDER_ID=founder-1
